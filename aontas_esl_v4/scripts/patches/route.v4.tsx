/**
 * aontas_esl_v4/scripts/patches/route.v4.tsx
 *
 * IMPORTANT:
 * Next.js "Running TypeScript" will typecheck files included by tsconfig.
 * This folder is for human-readable patch references, not runtime code.
 *
 * If you need to store a full API route implementation for copying,
 * store it as a .txt/.md file instead (so TS doesn't parse/typecheck it).
 */

export const ROUTE_V4_PATCH_NOTES = `
This file is intentionally TS-safe and non-executable.

The real route lives in:
- aontas_esl_v4/app/api/reading/generate-pack/route.tsx

If you want a copy of the route source here, create:
- aontas_esl_v4/scripts/patches/route.v4.txt
and paste the route text there.
`.trim();

export {};